services:
  tg-rss:
    build: .
    image: tg-rss:latest
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    env_file:
      - .env
