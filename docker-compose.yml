services:
  tg-rss:
    build: .
    image: tg-rss:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - tg-rss-data:/data
    env_file:
      - .env

volumes:
  tg-rss-data:
